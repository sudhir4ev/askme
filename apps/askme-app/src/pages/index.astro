---
import UploadPanel from "../components/composite/UploadPanel.astro";
import AppShell from "../components/layout/AppShell.astro";
import FooterBar from "../components/layout/FooterBar.astro";
import TopNav from "../components/layout/TopNav.astro";
import Card from "../components/ui/Card.astro";
import Layout from "../layouts/Layout.astro";

const navLinks = [
	{ key: "dashboard", label: "Dashboard", href: "#" },
	{ key: "reports", label: "Reports", href: "#" },
];
---

<Layout title="Initial Dependency | AI Reachability Analysis">
	<AppShell withTopbar={true} withFooter={true}>
		<Fragment slot="topbar">
			<TopNav links={navLinks} active="dashboard" searchPlaceholder="Scan projects..." />
		</Fragment>

		<section class="px-4 pt-9 pb-6 text-center">
			<h1 class="m-0 text-[clamp(2rem,6vw,3.4rem)] tracking-[-0.03em]">Initial Dependency</h1>
			<p class="mx-auto mt-3 max-w-[36rem] text-base text-[var(--color-text-secondary)]">
				AI-powered reachability analysis for your dependencies.
			</p>
		</section>

		<div class="mx-auto max-w-[52rem]">
			<UploadPanel />
		</div>

		<section class="mx-auto mt-9 grid max-w-[64rem] grid-cols-1 gap-3 md:grid-cols-2">
			<Card>
				<h3 class="mb-1 text-[0.92rem] uppercase tracking-[0.08em] text-[var(--color-primary)]">Deep Context</h3>
				<p class="m-0 text-[0.86rem] leading-[1.5] text-[var(--color-text-secondary)]">
					Beyond simple vulnerability matching, we analyze actual execution paths to determine if a vulnerable
					dependency is truly reachable.
				</p>
			</Card>
			<Card>
				<h3 class="mb-1 text-[0.92rem] uppercase tracking-[0.08em] text-[var(--color-primary)]">Smart Prioritization</h3>
				<p class="m-0 text-[0.86rem] leading-[1.5] text-[var(--color-text-secondary)]">
					Reduce security fatigue by filtering out noise and focus on initial dependencies creating real attack
					surfaces.
				</p>
			</Card>
		</section>

		<Fragment slot="footer">
			<FooterBar links={[{ label: "GitHub" }, { label: "Documentation" }, { label: "Privacy Policy" }]} />
		</Fragment>
	</AppShell>
</Layout>
