services:
  chromadb:
    image: chromadb/chroma:latest
    container_name: askme-chromadb
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - chromadb_data:/chroma/chroma

volumes:
  chromadb_data:
